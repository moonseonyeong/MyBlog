import { FlexBox, FlexColumnBox } from '@/styles/common';
import styled from '@emotion/styled';

export const Header = styled.h1`
  margin: 0;
`;
export const PostsContainer = styled(FlexColumnBox)`
  padding: 24px;
  cursor: pointer;
  border-radius: 20px;
`;
